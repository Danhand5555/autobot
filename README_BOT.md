# TTM Bot Execution Instructions

**JS Dashboard (Recommended)**

The bot uses Puppeteer to automate ThaiTicketMajor ticket booking.

```bash
cd ~/autobot
npm start
```

Then navigate to http://localhost:3000 to configure and run the bot.
